- name: logstash configuration copy
  ansible.builtin.template:
    src: logstash.conf
    dest: /etc/logstash/conf.d/logstash.conf

- name: restart logstash configuration
  ansible.builtin.service:
    name: logstash
    state: restarted
    enabled: yes