- name: copy prometheus configuration
  ansible.builtin.template:
    src: prometheus.yml
    dist: /opt/prometheus/prometheus.yml

- name: restart prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restart
